# Ignored (legacy archive)

This subfolder keeps older benchmark assets that were excluded from the active experiment pipeline.

- `fasta/`: archived benchmark files and measurement JSON from a previous setup

Keep this folder as historical reference only.
