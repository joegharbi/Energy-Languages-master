# old_measurements (legacy)

This folder stores historical measurement exports from earlier experiment rounds.

## Contents

- Per-language output snapshots (`c_output.csv`, `erlang_output.csv`, `go_output.csv`, `java_output.csv`, `2go_output.csv`)
- `output.csv`: merged/combined historical export
- `Ignored/`: archived legacy benchmark folder kept for traceability

## Status

Legacy archive. Keep these files for historical comparison and publication traceability; do not use this folder as the main active output location for new runs.
